{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "react-dev": "cd resources/react && npm start",
        "react-build": "cd resources/react && npm run build",
        "postinstall": "cd resources/react && npm install",
        "deploy": "composer install --optimize-autoloader --no-dev && npm run build && npm run react-build && php artisan config:cache && php artisan route:cache && php artisan view:cache",
        "deploy-react": "cd resources/react && npm run build && xcopy /E /I /Y build\\* ..\\..\\public\\"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14",
        "vite": "^4.0.0"
    }
}
